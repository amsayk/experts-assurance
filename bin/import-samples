#!/usr/bin/env node

import Parse from 'parse/node';

const log = require('log')('app:bin:samples');

(async function () {
  // log('Importing samples');
  // try {
  //   await Promise.all([
  //     Parse.Cloud.run('importSamples', {}),
  //   ]);
  //   log('Samples successfully imported.');
  // } catch (e) {
  //   log.error('Error while importing samples:', e);
  //   throw e;
  // }

    log.error('Importing samples retired. It will be removed in the near future.');
})()

