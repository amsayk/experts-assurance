query getImportation($id: ID!) {
  getImportation(id: $id) {
    id
    docs
    files {
      name
    }
    date
    endDate

    progress
    total

    error {
      code
      message
      stack
    }

    user {
      id
      displayName
    }

  }
}


