query morePendingDocs($durationInDays: Float!, $cursor: Int!, $sortConfig: ESSortConfig!) {
  pendingDashboard(durationInDays: $durationInDays, cursor: $cursor, sortConfig: $sortConfig) {
    cursor
    docs {
      id
      refNo
      date
      dateMission
      company
      state
      vehicle {
        manufacturer
        model
        plateNumber
        series
        mileage
        DMC
        energy
        power
      }
      client {
        id
        displayName
      }
      manager {
        id
        displayName
      }

      validation {
        date
        amount
        user {
          id
          displayName
        }
      }

      payment : paymentInfo {
        user { id }
        amount
        date
      }

      createdAt
      updatedAt
      lastModified
    }
  }
}

