query getTimeline($cursor: Date, $query: TimelineQuery!) {
  timeline(cursor: $cursor, query: $query) {
    cursor
    result {
      id,
      document {
        id
        refNo
        state
        dateMission
        # validation {
        #   date
        #   user {
        #     id
        #     displayName
        #   }
        # }
        # closure {
        #   date
        #   state
        #   user {
        #     id
        #     displayName
        #   }
        # }
        # user {
        #   id
        #   displayName
        # }
      }
      file {
        id
        name
        category
        type
        url

        # user {
        #   id
        #   displayName
        # }

        date
      }
      ns
      type
      metadata
      timestamp
      user {
        id
        displayName
      }
    }
  }
}

