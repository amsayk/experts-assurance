query landing__currentUser($id: ID!) {

  currentUser : getUser(id: $id) {
    id
    displayName
    email
    username
    roles
    emailVerified

    createdAt
    updatedAt

    business {
      id
    }
  }
}

