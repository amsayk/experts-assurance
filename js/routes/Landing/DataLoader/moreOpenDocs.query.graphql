query moreOpenDocs($durationInDays: Float!, $cursor: Int!, $sortConfig: ESSortConfig!) {
  openDashboard(durationInDays: $durationInDays, cursor: $cursor, sortConfig: $sortConfig) {
    cursor
    docs {
      id
      refNo
      date
      dateMission
      state
      vehicle {
        model
        plateNumber
      }
      validation {
        date
        user {
          id
          displayName
        }
      }
      client {
        id
        displayName
      }
      manager {
        id
        displayName
      }

      payment : paymentInfo {
        user { id }
        amount
        date
      }

      createdAt
      updatedAt
      lastModified
    }
  }
}

