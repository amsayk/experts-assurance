query pendingDocs($durationInDays: Int!, $sortConfig: ESSortConfig!) {
  pendingDashboard(durationInDays: $durationInDays, sortConfig: $sortConfig) {
    length
    cursor
    docs {
      id
      refNo
      date
      dateMission
      state
      vehicle {
        model
        plateNumber
      }
      client {
        id
        displayName
      }
      manager {
        id
        displayName
      }

      payment : paymentInfo {
        user { id }
        amount
        date
      }

      createdAt
      updatedAt
      lastModified
    }
  }
}


