query landing__getUser($id: ID!) {

  currentUser : getUser(id: $id) {
    id
    displayName
    email
    username
    emailVerified

    createdAt
    updatedAt

    business {
      id
    }
  }
}

