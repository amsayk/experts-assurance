query esQueryDocs($query: ESDocsQueryPayload!) {
  esQueryDocs(query: $query) {
    took
    length
    cursor
    hits {
      _id
      highlight
      _source {
        id
        manager{ id name }
        agent{ id name }
        user{ id name }
        client{ id name }
        refNo
        state
        vehicle{
          manufacturer
          model
          plateNumber
        }
        validation {
          user { id name }
          date
        }
        closure {
          state
          user { id name }
          date
        }
        date
        lastModified
      }
    }
  }
}
